# audiofs_events_dump bench tool for F.3.d (ADR 0017) and
# follow-on F-stages. Reads /var/run/sema/audio/events and
# prints decoded events.

PROG=		audiofs_events_dump
SRCS=		audiofs_events_dump.c
CFLAGS+=	-O2 -Wall

# No man page in v1; same convention as playtone.
MAN=

.include <bsd.prog.mk>

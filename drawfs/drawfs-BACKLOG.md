# drawfs (kernel spatial substrate) — backlog

**This backlog has been consolidated into the root `BACKLOG.md`.**

All tracked work for drawfs (kernel spatial substrate) now lives in [`../BACKLOG.md`](../BACKLOG.md),
under the **`drawfs` — kernel spatial substrate** section. Item IDs are preserved (DF-1, DF-2, DF-3).

This file remains as a pointer so existing links, references, and tooling
continue to resolve. Do not add new tasks here — add them to the root
backlog.

See the root backlog's "How to read this" section for the status and
priority conventions.
